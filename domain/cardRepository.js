class CardRepository {
  constructor(model) {
    this.model = model;
  }

  save(card) {
    return;
  }

  getByUserId(userId) {
    return;
  }

  isFirstCard(userId) {
    return;
  }
}

export default CardRepository;
